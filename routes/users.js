/*const express = require("express");
const { ListUsers } = require("../controllers/user_controller");
const router = express.Router();
router.get("/ListUsers", ListUsers);
module.exports = router;
*/
