/*import { USER_LIST } from "../types/admin_user_list";

const initialstate = {
  user: {},
};

const userlistReducer = (state = initialstate, action) => {
  switch (action.type) {
    case USER_LIST:
      return { ...state, user: action.payload };
    default:
      return state;
  }
};

export default userlistReducer;
*/
